<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MobilePrograming" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/gray</item>
    </style>

    <style name="Theme.MobilePrograming" parent="Base.Theme.MobilePrograming" />
    <style name="Theme.HomeActivity" parent="Base.Theme.MobilePrograming" />

    <style name="card_menu_home" parent="Base.Theme.MobilePrograming" >
        <item name="cardCornerRadius">10dp</item>
        <item name="android:layout_height">80dp</item>
        <item name="cardElevation">10dp</item>
        <item name="android:layout_margin">10dp</item>
    </style>

    <style name="text_card_menu_home" parent="Base.Theme.MobilePrograming" >
        <item name="android:textSize">22sp</item>
        <item name="android:textStyle">normal</item>
    </style>

</resources>